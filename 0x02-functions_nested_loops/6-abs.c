#include "main.h"
#include <stdlib.h>
/**
 * _abs - computes the absolute of a number
 * @num: type int parameter
 * Return: the absolute number
 */
int _abs(int num)
{
	num = abs(num);
	return (num);
}
