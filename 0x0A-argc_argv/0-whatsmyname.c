#include <stdio.h>
/**
 * main - entry point
 * @argc: no. of argms
 * @argv: argument vector
 * Return: return 0 (Success)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - 1]);
	return (0);
}
